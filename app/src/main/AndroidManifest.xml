<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.leftshift.materialtryout">

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

            <activity android:name=".activities.TransitionFirstActivity"
                android:theme="@style/TransitionSampleActivity">


            </activity>

            <activity
                android:name=".activities.TransitionSecondActivity"
                android:theme="@style/TransitionSampleSecondActivity">
                </activity>

            <activity android:name=".activities.SlidingActivity" >

            </activity>
            <activity
                android:name=".activities.DialerSampleActivity"
                android:screenOrientation="portrait" >
                </activity>
            <activity
                android:name=".activities.ColorActivity"
                android:label="@string/title_activity_color" >
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>

                </activity>
            <activity
                android:name=".activities.ElevationSampleActivity"
                android:label="@string/title_activity_elevation_sample" >


            </activity>

    </application>

</manifest>
